box: ubuntu
build:
  steps:
    - install-packages:
      packages: curl
    - jyotti/install-packer:
      version: 1.2.1
    - script:
        name: check packer
        code: |
          packer -v
          ls
          