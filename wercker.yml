box: ubuntu
build:
  steps:
    - install-packages:
      packages: curl
    - jyotti/install-packer:
      version: 1.2.1
    - script:
        name: install packer
        code: |
          apt-get install git