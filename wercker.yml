box: ubuntu
build:
  steps:
    - install-packages:
      packages: curl
    - jyotti/install-packer:
      version: 1.2.1
    - script:
        name: run packer build for windows10base
        code: |
          cd .\windows10-base
          packer build packer.json
